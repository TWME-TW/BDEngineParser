/**
 * Contains data objects representing the core structures of the BD Engine project.
 * These classes define the model layer, including elements, transforms, brightness, and text options.
 */
package dev.twme.bdengineparser.model;
